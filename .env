# rutas locales
LOCAL_STORAGE_DIR=./data
DB_URL=sqlite:///./local.db

# índices (se usan recién en el Sprint 2, pero los definimos ya)
WHOOSH_DIR=./indices/whoosh
FAISS_INDEX_PATH=./indices/course.faiss

# TTS (opcional)
USE_TTS=false
TTS_ENGINE=piper

# Gemini (única pieza no-local)
GEMINI_API_KEY=tu_api_key
GEMINI_MODEL=gemini-2.0-flash   # o el que uses
